*Practice GraphQL with node and express*
 
GraphQL is a wonder tool that allow the user that consume an API endpoint define the kind of data she desire to extract and also define citeria for such data
